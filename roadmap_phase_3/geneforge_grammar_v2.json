{
  "GENOMIC_EDITING_BLOCKS": {
    "ENZYME": {
      "params": ["name", "type", "species", "variant", "strand", "pam", "notes"]
    },
    "OLIGO": {
      "params": ["sequence", "type", "length", "mod", "notes"]
    },
    "EDIT": {
      "params": ["type", "target", "pos", "ref", "alt", "exons", "introns", "splice", "efficiency", "byproduct", "notes"]
    },
    "DELIVERY": {
      "params": ["vehicle", "payload", "cells", "tissue", "method", "dose", "units", "schedule", "notes"]
    },
    "CONTEXT": {
      "params": ["organism", "tissue", "cell", "condition", "env", "notes"]
    }
  },
  "MOLECULAR_INTERACTIONS": {
    "INTERACT": {
      "params": ["partner1", "partner2", "type", "strength", "context", "notes"]
    },
    "POSTTRANS": {
      "params": ["type", "affected", "method", "notes"]
    }
  },
  "LOGIC_AND_SIMULATION": {
    "IF": {
      "syntax": "IF(condition) { ... } ELSE { ... }"
    },
    "LOOP": {
      "syntax": "LOOP(n) { ... }"
    },
    "SIMULATE": {
      "params": ["action", "times", "model", "params", "notes"]
    },
    "BENCHMARK": {
      "params": ["ref_case", "dataset", "metric", "notes"]
    },
    "TRACE": {
      "params": ["reasoning", "result", "confidence", "alternatives", "path", "notes"]
    }
  }
}
