plan:
  goal: "Este plan es inválido."
  steps:
    - name: good_step
      thought: "Este paso está bien."
      type: log_message
      attrs:
        message: "Hola"

    - name: bad_step
      thought: "Este paso hace referencia a un paso que no existe."
      type: log_message
      attrs:
        message: "El resultado del paso inexistente es ${non_existent_step.result.value}"
